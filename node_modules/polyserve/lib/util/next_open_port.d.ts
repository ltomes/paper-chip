/**
 * If port unspecified/negative, finds an open port on localhost
 * @param {number} port
 * @returns {Promise<number>} Promise of open port
 */
export declare function nextOpenPort(port: number): Promise<number>;
